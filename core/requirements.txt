# Packages actually imported by the core Flask service.
# The original pip-freeze dump (with jax, tensorflow, opencv, etc.) has been
# preserved in requirements.full.txt for reference but is not needed to run this app.

Flask>=3.0.3
Werkzeug>=3.0.4
groq>=0.11.0
ibm-watson>=8.1.0
ibm-cloud-sdk-core>=3.21.0
requests>=2.32.3
python-dotenv>=1.0.1
